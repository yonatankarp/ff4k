{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"FF4K - Feature Flags for Kotlin","text":"<p>FF4K is a Kotlin Multiplatform (KMP) implementation of the popular FF4J (Feature Flipping for Java) library. It brings robust feature flagging capabilities to the Kotlin ecosystem, supporting multiplatform projects.</p>"},{"location":"#key-features","title":"Key Features","text":"<ul> <li>Kotlin Multiplatform: Designed to work across different platforms supported by Kotlin (JVM, Android, Native, etc.).</li> <li>Type-safe Properties: Strongly typed property definitions (String, Int, Boolean, etc.).</li> <li>Flipping Strategies: Gradual rollouts, A/B testing, and user targeting with built-in strategies.</li> <li>Serialization Support: Built-in support for <code>kotlinx.serialization</code>.</li> <li>Extensible: Easily implement custom storage backends (Redis, SQL, etc.) and custom strategies.</li> <li>DSL: Intuitive domain-specific language for configuration.</li> </ul>"},{"location":"#installation","title":"Installation","text":""},{"location":"#using-bill-of-materials-bom-recommended","title":"Using Bill of Materials (BOM) - Recommended","text":"<p>The BOM ensures that all FF4K modules are using compatible versions.</p> <pre><code>repositories {\n    mavenCentral()\n}\n\ndependencies {\n    // Import the BOM\n    implementation(platform(\"com.yonatankarp:ff4k-bom:&lt;version&gt;\"))\n\n    // Add dependencies without versions\n    implementation(\"com.yonatankarp:ff4k-core\")\n}\n</code></pre>"},{"location":"#manual-versioning","title":"Manual Versioning","text":"<p>Alternatively, you can specify the version for each module directly:</p> <pre><code>repositories {\n    mavenCentral()\n}\n\ndependencies {\n    implementation(\"com.yonatankarp:ff4k-core:&lt;version&gt;\")\n}\n</code></pre>"},{"location":"#quick-start","title":"Quick Start","text":"<pre><code>suspend fun main() {\n    val ff4k = ff4k {\n        features {\n            feature(\"dark-mode\") {\n                isEnabled = true\n                description = \"Enable dark mode theme\"\n            }\n        }\n    }\n\n    // Check feature status\n    ff4k.ifEnabled(\"dark-mode\") {\n        // ...\n    }\n}\n</code></pre> <p>Check out the Usage guide for more details.</p>"},{"location":"#license","title":"License","text":"<p>This project is licensed under the Apache License 2.0.</p>"},{"location":"api_reference/","title":"API Reference","text":"<p>The full API reference documentation is generated via Dokka.</p> <p>Click here to view the API documentation</p>"},{"location":"packages/","title":"Packages","text":"<p>FF4K is modularized into several packages to allow for flexible usage and extension.</p> Package Description Platform Support <code>ff4k-bom</code> The Bill of Materials (BOM) helps you manage versions of FF4K libraries. By importing the BOM, you ensure that all FF4K dependencies in your project use the same version. <code>ff4k-contract-test</code> Contains contract tests (like <code>FeatureStoreContractTest</code>) to verify that custom implementations of FF4K interfaces (e.g., custom Feature Stores) adhere to the expected behavior. <code>ff4k-core</code> The core library containing the main API, configuration logic, default stores, and standard strategies. This is the primary dependency for using FF4K. <code>ff4k-store-jdbc</code> A blocking JDBC feature store implementation. Ideal for traditional blocking JVM applications. See supported databases. <code>ff4k-store-r2dbc</code> A non-blocking, reactive R2DBC feature store implementation. Ideal for reactive applications (e.g., Spring WebFlux). See supported databases. <code>ff4k-store-sqlite</code> A Multiplatform SQLite feature store implementation. Supports JVM, Android, and iOS/Native targets."},{"location":"packages/#database-requirements","title":"Database Requirements","text":"<p>When using SQL-based stores, the following minimum database versions are required:</p> Database Minimum Version Notes MySQL 8.0.19+ Required for row alias syntax in <code>INSERT ... AS</code> upserts PostgreSQL 9.5+ Required for <code>ON CONFLICT</code> upsert syntax SQLite 3.24.0+ Required for <code>ON CONFLICT</code> upsert syntax"},{"location":"customization/","title":"Extending FF4K","text":"<p>FF4K is designed to be extensible. You can implement custom storage backends for features and properties to persist your flags in databases, remote services (like Redis, Consul), or local files.</p> <p>You can also create custom flipping strategies to control feature availability based on your specific business logic.</p> <p>Explore the guides below to learn how to extend FF4K:</p> <ul> <li>Custom Feature Store</li> <li>Custom Property Store</li> <li>Custom Flipping Strategy</li> </ul>"},{"location":"customization/custom-feature-store/","title":"Implementing a Feature Store","text":"<p>To create a custom feature store, implement the <code>FeatureStore</code> interface. For convenience, it is highly recommended to extend <code>AbstractFeatureStore</code>, which handles most of the common logic and boilerplate.</p>"},{"location":"customization/custom-feature-store/#using-abstractfeaturestore","title":"Using AbstractFeatureStore","text":"<p><code>AbstractFeatureStore</code> provides default implementations for group management, permission handling helper methods, and validation. You primarily need to implement the core CRUD operations.</p> <pre><code>class MyCustomFeatureStore : AbstractFeatureStore() {\n\n    // Helper map to simulate a DB for this example\n    private val db = mutableMapOf&lt;String, Feature&gt;()\n\n    override suspend fun get(featureId: String): Feature? {\n        return db[featureId]\n    }\n\n    override suspend fun getAll(): Map&lt;String, Feature&gt; {\n        return db.toMap()\n    }\n\n    // Using the 'plusAssign' operator for creation (store += feature)\n    override suspend fun plusAssign(feature: Feature) {\n        if (feature.uid in db) {\n            throw FeatureAlreadyExistsException(feature.uid)\n        }\n        db[feature.uid] = feature\n    }\n\n    // Using the 'minusAssign' operator for deletion (store -= featureId)\n    override suspend fun minusAssign(featureId: String) {\n         if (featureId !in db) {\n            throw FeatureNotFoundException(featureId)\n        }\n        db.remove(featureId)\n    }\n\n    override suspend fun update(feature: Feature) {\n         if (feature.uid !in db) {\n            throw FeatureNotFoundException(feature.uid)\n        }\n        db[feature.uid] = feature\n    }\n\n    override suspend fun clear() {\n        db.clear()\n    }\n\n    // ... implement other abstract methods ...\n}\n</code></pre>"},{"location":"customization/custom-feature-store/#verifying-your-implementation","title":"Verifying Your Implementation","text":"<p>It is critical to ensure your custom store behaves correctly. FF4K provides a Contract Test Suite that you can use to automatically verify your implementation against the expected behavior.</p> <pre><code>class MyCustomFeatureStoreTest : FeatureStoreContractTest() {\n    override suspend fun createStore(): FeatureStore {\n        // Return a fresh instance of your store for each test\n        return MyCustomFeatureStore()\n    }\n}\n</code></pre> <p>This will run a suite of tests covering CRUD operations, toggling features, group operations, permissions, and concurrency.</p>"},{"location":"customization/custom-feature-store/#registering-your-store","title":"Registering Your Store","text":"<p>Once implemented, pass your custom store to the <code>ff4k</code> configuration function.</p> <pre><code>suspend fun main() {\n    val ff4k = ff4k(\n        featureStore = MyCustomFeatureStore(),\n        // ...\n    ) {\n        // ...\n    }\n}\n</code></pre>"},{"location":"customization/custom-property-store/","title":"Implementing a Property Store","text":"<p>To create a custom property store, implement the <code>PropertyStore</code> interface.</p> <pre><code>class MyCustomPropertyStore : PropertyStore {\n   // Implement interface...\n}\n</code></pre>"},{"location":"customization/custom-property-store/#verifying-your-implementation","title":"Verifying Your Implementation","text":"<p>It is critical to ensure your custom store behaves correctly. FF4K provides a Contract Test Suite that you can use to automatically verify your implementation against the expected behavior.</p> <pre><code>class MyCustomPropertyStoreTest : PropertyStoreContractTest() {\n    override suspend fun createStore(): PropertyStore {\n        // Return a fresh instance of your store for each test\n        return MyCustomPropertyStore()\n    }\n}\n</code></pre> <p>This will run a suite of tests covering CRUD operations, concurrency, and error handling.</p>"},{"location":"customization/custom-property-store/#registering-your-store","title":"Registering Your Store","text":"<p>Once implemented, pass your custom store to the <code>ff4k</code> configuration function.</p> <pre><code>suspend fun main() {\n    val ff4k = ff4k(\n        propertyStore = MyCustomPropertyStore()\n        // ...\n    ) {\n        // ...\n    }\n}\n</code></pre>"},{"location":"customization/custom-strategy/","title":"Implementing a Flipping Strategy","text":"<p>To create a custom flipping strategy, implement the <code>FlippingStrategy</code> interface. For an overview of built-in strategies, see Flipping Strategies.</p> <pre><code>@Serializable\n@SerialName(\"timeBasedStrategy\")\ndata class TimeBasedStrategy(\n    val startHour: Int,\n    val endHour: Int\n) : FlippingStrategy {\n    override suspend fun evaluate(\n        featureId: String,\n        store: FeatureStore?,\n        context: FlippingExecutionContext\n    ): Boolean {\n        val currentHour = Clock.System.now()\n            .toLocalDateTime(TimeZone.currentSystemDefault())\n            .hour\n        return currentHour in startHour until endHour\n    }\n}\n</code></pre>"},{"location":"customization/custom-strategy/#verifying-your-implementation","title":"Verifying Your Implementation","text":"<p>It is critical to ensure your custom strategy behaves correctly. FF4K provides a Contract Test Suite that you can use to automatically verify your implementation against the expected behavior.</p> <pre><code>class TimeBasedStrategyTest : FlippingStrategyContractTest() {\n\n    // Create a strategy that should PASS given the current conditions (or context)\n    override fun createStrategyForPassingCase(): FlippingStrategy {\n        // Assuming we mock the clock or configure the strategy to span the current time\n        return TimeBasedStrategy(startHour = 0, endHour = 24) \n    }\n\n    // Create a strategy that should FAIL\n    override fun createStrategyForFailingCase(): FlippingStrategy {\n        // 23 until 1 creates an empty range in Kotlin, so this will always fail\n        return TimeBasedStrategy(startHour = 23, endHour = 1)\n    }\n\n    // Provide context if your strategy relies on it (optional for time-based)\n    override fun contextThatShouldPass() = FlippingExecutionContext()\n    override fun contextThatShouldFail() = FlippingExecutionContext()\n\n    // Verify JSON serialization\n    override fun expectedJsonForSampleParams(): String {\n        return \"\"\"{\"type\":\"timeBasedStrategy\",\"startHour\":0,\"endHour\":24}\"\"\"\n    }\n}\n</code></pre>"},{"location":"customization/custom-strategy/#registering-for-serialization","title":"Registering for Serialization","text":"<p>If you need JSON serialization support for your custom strategy, register it in a custom serializers module and combine it with the built-in module. See Serialization for more details on <code>ff4kSerializersModule</code>.</p> <pre><code>val customSerializersModule = SerializersModule {\n    polymorphic(FlippingStrategy::class) {\n        subclass(TimeBasedStrategy::class, TimeBasedStrategy.serializer())\n    }\n}\n\nval json = Json {\n    serializersModule = ff4kSerializersModule + customSerializersModule\n}\n</code></pre>"},{"location":"customization/testing/","title":"Contract Testing","text":"<p>The <code>ff4k-contract-test</code> module provides a suite of reusable tests to verify that your custom <code>FeatureStore</code> and <code>PropertyStore</code> implementations adhere to the FF4K specification. This ensures consistency and reliability across different storage backends.</p>"},{"location":"customization/testing/#setup","title":"Setup","text":"<p>First, add the contract test dependency to your project.</p> <p>Gradle (Kotlin DSL)</p> <p>If you are using the FF4K BOM, you don't need to specify the version:</p> <pre><code>dependencies {\n    testImplementation(\"com.yonatankarp:ff4k-contract-test\")\n}\n</code></pre> <p>Otherwise, specify the version explicitly:</p> <pre><code>dependencies {\n    testImplementation(\"com.yonatankarp:ff4k-contract-test:&lt;version&gt;\")\n}\n</code></pre>"},{"location":"customization/testing/#testing-a-feature-store","title":"Testing a Feature Store","text":"<p>To test your custom <code>FeatureStore</code>, create a test class that extends <code>FeatureStoreContractTest</code> and implement the <code>createStore</code> method.</p> <pre><code>class MyCustomFeatureStoreTest : FeatureStoreContractTest() {\n\n    // This method is called before each test to provide a fresh store instance\n    override suspend fun createStore(): FeatureStore {\n        // Return a fresh instance of your store\n        // If your store relies on an external DB, ensure it's cleaned up here\n        return MyCustomFeatureStore()\n    }\n}\n</code></pre> <p>The <code>FeatureStoreContractTest</code> will automatically run a comprehensive set of tests covering: - basic CRUD (Create, Read, Update, Delete) - toggling features - group operations (enable/disable group, add/remove from group) - permissions - error handling (e.g., throwing correct exceptions for missing features) - concurrency handling</p>"},{"location":"customization/testing/#testing-a-property-store","title":"Testing a Property Store","text":"<p>Similar to features, you can test your custom <code>PropertyStore</code> by extending <code>PropertyStoreContractTest</code>.</p> <pre><code>class MyCustomPropertyStoreTest : PropertyStoreContractTest() {\n    override suspend fun createStore(): PropertyStore {\n        return MyCustomPropertyStore()\n    }\n}\n</code></pre> <p>This ensures your property store correctly handles CRUD operations for properties, type safety, and concurrency.</p>"},{"location":"customization/testing/#testing-properties","title":"Testing Properties","text":"<p>To test custom <code>Property</code> implementations, you can extend <code>PropertyContractTest</code>.</p> <pre><code>class MyPropertyTest : PropertyContractTest&lt;String, MyStringProperty&gt;() {\n    override val serializer = MyStringProperty.serializer()\n\n    override fun create(\n        name: String,\n        value: String,\n        description: String?,\n        fixedValues: Set&lt;String&gt;,\n        readOnly: Boolean\n    ) = MyStringProperty(name, value, description, fixedValues, readOnly)\n\n    override fun sampleName() = \"test-prop\"\n    override fun sampleValue() = \"value\"\n    override fun otherValueNotInFixedValues() = \"other-value\"\n    override fun fixedValuesIncludingSample(sample: String) = setOf(sample, \"other\")\n}\n</code></pre>"},{"location":"customization/testing/#testing-flipping-strategies","title":"Testing Flipping Strategies","text":"<p>To test custom <code>FlippingStrategy</code> implementations, extend <code>FlippingStrategyContractTest</code> and implement the required methods.</p> <pre><code>class MyCustomStrategyTest : FlippingStrategyContractTest() {\n\n    // Create a strategy instance configured to pass with contextThatShouldPass()\n    override fun createStrategyForPassingCase(): FlippingStrategy {\n        return MyCustomStrategy(configuredToPass = true)\n    }\n\n    // Create a strategy instance configured to fail with contextThatShouldFail()\n    override fun createStrategyForFailingCase(): FlippingStrategy {\n        return MyCustomStrategy(configuredToPass = false)\n    }\n\n    // Context that should result in true when used with createStrategyForPassingCase()\n    override fun contextThatShouldPass(): FlippingExecutionContext {\n        return FlippingExecutionContext(ContextKeys.USER_ID to \"allowed-user\")\n    }\n\n    // Context that should result in false when used with createStrategyForFailingCase()\n    override fun contextThatShouldFail(): FlippingExecutionContext {\n        return FlippingExecutionContext(ContextKeys.USER_ID to \"denied-user\")\n    }\n\n    // Expected JSON serialization for the strategy from createStrategyForPassingCase()\n    override fun expectedJsonForSampleParams(): String {\n        return \"\"\"{\"type\":\"myCustom\",\"configuredToPass\":true}\"\"\"\n    }\n}\n</code></pre> <p>The contract test provides separate strategy creation methods (<code>createStrategyForPassingCase</code> and <code>createStrategyForFailingCase</code>) to support strategies that depend on injected dependencies like clocks or random sources. For context-driven strategies where the same instance can both pass and fail based on context, both methods can return the same strategy instance.</p> <p>The <code>FlippingStrategyContractTest</code> will automatically run tests covering: - Evaluation returns true for passing case - Evaluation returns false for failing case - Handling of null feature store - JSON serialization</p>"},{"location":"customization/testing/#running-tests","title":"Running Tests","text":"<p>Run your tests as you normally would use Gradle:</p> <pre><code>./gradlew test\n</code></pre>"},{"location":"stores/","title":"Feature Stores","text":"<p>FF4K supports multiple storage backends for feature flags.</p> Store Module Platforms Supported Databases JDBC <code>ff4k-store-jdbc</code> JVM PostgreSQL, MySQL R2DBC <code>ff4k-store-r2dbc</code> JVM Coming soon SQLite <code>ff4k-store-sqlite</code> JVM, Android, iOS, Native SQLite <p>See database requirements for minimum versions.</p>"},{"location":"stores/jdbc/","title":"JDBC Feature Store","text":"<p>The <code>ff4k-store-jdbc</code> module provides a JDBC-based <code>FeatureStore</code> implementation for relational databases on the JVM.</p>"},{"location":"stores/jdbc/#supported-databases","title":"Supported Databases","text":"<ul> <li>PostgreSQL</li> <li>MySQL</li> </ul>"},{"location":"stores/jdbc/#installation","title":"Installation","text":"<p>Add the dependency and your database driver:</p> <pre><code>dependencies {\n    implementation(\"com.yonatankarp:ff4k-store-jdbc:&lt;version&gt;\")\n\n    // Database driver (choose one)\n    runtimeOnly(\"org.postgresql:postgresql:&lt;version&gt;\")\n    runtimeOnly(\"com.mysql:mysql-connector-j:&lt;version&gt;\")\n}\n</code></pre>"},{"location":"stores/jdbc/#usage","title":"Usage","text":""},{"location":"stores/jdbc/#basic-usage","title":"Basic Usage","text":"<p>Pass a <code>DataSource</code> to create a store. The database type is detected automatically:</p> <pre><code>val dataSource = HikariDataSource(config)\nval store = jdbcFeatureStore(dataSource)\n\nstore += Feature(uid = \"my-feature\", isEnabled = true)\n</code></pre>"},{"location":"stores/jdbc/#explicit-dialect","title":"Explicit Dialect","text":"<p>For database proxies or when auto-detection fails, use <code>&lt;DatabaseName&gt;Dialect</code>:</p> <pre><code>val store = jdbcFeatureStore(dataSource, PostgresDialect)\n</code></pre>"},{"location":"stores/jdbc/#custom-serializers","title":"Custom Serializers","text":"<p>For custom <code>FlippingStrategy</code> or <code>Property</code> implementations:</p> <pre><code>val customModule = SerializersModule {\n    polymorphic(FlippingStrategy::class) {\n        subclass(MyCustomStrategy::class)\n    }\n}\n\nval store = jdbcFeatureStore(dataSource, customModule)\n</code></pre>"},{"location":"stores/jdbc/#features","title":"Features","text":"<ul> <li>Automatic schema creation - Table created on first use</li> <li>Optimistic locking - Safe concurrent updates with retry</li> <li>Connection pooling - Bring your own <code>DataSource</code> (HikariCP recommended)</li> </ul>"},{"location":"stores/r2dbc/","title":"R2DBC Feature Store","text":"<p>Coming soon.</p>"},{"location":"stores/sqlite/","title":"SQLite Feature Store","text":"<p>The <code>ff4k-store-sqlite</code> module provides a SQLite-based <code>FeatureStore</code> and <code>PropertyStore</code> implementation using SQLDelight.</p>"},{"location":"stores/sqlite/#supported-platforms","title":"Supported Platforms","text":"<ul> <li>JVM</li> <li>Android</li> <li>iOS / Native</li> </ul>"},{"location":"stores/sqlite/#installation","title":"Installation","text":"<pre><code>dependencies {\n    implementation(\"com.yonatankarp:ff4k-store-sqlite:&lt;version&gt;\")\n}\n</code></pre>"},{"location":"stores/sqlite/#usage","title":"Usage","text":""},{"location":"stores/sqlite/#jvm","title":"JVM","text":"<pre><code>val driver = JdbcSqliteDriver(\"jdbc:sqlite:ff4k.db\")\nSqliteDatabase.Schema.create(driver).await()\n\nval featureStore = SqliteFeatureStore(driver)\nval propertyStore = SqlitePropertyStore(driver)\n</code></pre>"},{"location":"stores/sqlite/#android","title":"Android","text":"<pre><code>val driver = AndroidSqliteDriver(\n    schema = SqliteDatabase.Schema,\n    context = context,\n    name = \"ff4k.db\"\n)\n\nval featureStore = SqliteFeatureStore(driver)\nval propertyStore = SqlitePropertyStore(driver)\n</code></pre>"},{"location":"stores/sqlite/#ios-native","title":"iOS / Native","text":"<pre><code>val driver = NativeSqliteDriver(SqliteDatabase.Schema, \"ff4k.db\")\n\nval featureStore = SqliteFeatureStore(driver)\nval propertyStore = SqlitePropertyStore(driver)\n</code></pre>"},{"location":"stores/sqlite/#custom-serializers","title":"Custom Serializers","text":"<p>For custom <code>FlippingStrategy</code> or <code>Property</code> implementations:</p> <pre><code>val customModule = SerializersModule {\n    polymorphic(FlippingStrategy::class) {\n        subclass(MyCustomStrategy::class)\n    }\n}\n\nval featureStore = SqliteFeatureStore(driver, customModule)\nval propertyStore = SqlitePropertyStore(driver, customModule)\n</code></pre>"},{"location":"stores/sqlite/#features","title":"Features","text":"<ul> <li>Multiplatform - JVM, Android, iOS, and Native from a single module</li> <li>Automatic schema creation - Tables and indexes created via SQLDelight migrations</li> <li>Optimistic locking - Safe concurrent updates with retry</li> <li>Property store - Stores both feature flags and typed properties</li> </ul>"},{"location":"usage/basics/","title":"Usage Guide","text":"<p>This guide covers the core concepts and usage patterns of FF4K.</p>"},{"location":"usage/basics/#initialization","title":"Initialization","text":"<p>The entry point for FF4K is the <code>ff4k</code> DSL function. It allows you to configure features and properties and returns an <code>FF4k</code> instance.</p> <pre><code>suspend fun main() {\n    val ff4k = ff4k {\n        // Configuration block\n    }\n}\n</code></pre>"},{"location":"usage/basics/#configuration-options","title":"Configuration Options","text":"<p>The <code>ff4k</code> function accepts several optional arguments to customize behavior:</p> <pre><code>suspend fun main() {\n    val ff4k = ff4k(\n        autoCreate = true,                // Automatically create missing features (default: false)\n        featureStore = InMemoryFeatureStore(), // Custom feature store (default: InMemory)\n        propertyStore = InMemoryPropertyStore() // Custom property store (default: InMemory)\n    ) {\n        // ...\n    }\n}\n</code></pre> <ul> <li><code>autoCreate</code>: If set to <code>true</code>, querying a feature that doesn't exist will automatically create it in the store (disabled by default).</li> <li><code>featureStore</code>: The backend storage for feature flags.</li> <li><code>propertyStore</code>: The backend storage for properties.</li> </ul>"},{"location":"usage/basics/#defining-features","title":"Defining Features","text":"<p>Features are boolean flags that can be toggled on or off. You can define them within the <code>features</code> block.</p> <pre><code>suspend fun main() {\n    val ff4k = ff4k {\n        features {\n            // Minimal definition\n            feature(\"simple-feature\")\n\n            // Detailed definition\n            feature(\"advanced-feature\") {\n                isEnabled = true\n                description = \"Controls the new dashboard layout\"\n                group = \"ui-beta\"\n                permissions(\"ADMIN\", \"BETA_TESTER\")\n            }\n        }\n    }\n}\n</code></pre>"},{"location":"usage/basics/#feature-attributes","title":"Feature Attributes","text":"<ul> <li><code>uid</code>: Unique identifier for the feature.</li> <li><code>isEnabled</code>: Initial state of the feature (default: <code>false</code>).</li> <li><code>description</code>: Human-readable description.</li> <li><code>group</code>: Group name for organizing features.</li> <li><code>permissions</code>: List of roles/permissions required to access the feature.</li> </ul>"},{"location":"usage/basics/#defining-properties","title":"Defining Properties","text":"<p>Properties are key-value pairs that can store configuration data. They are strongly typed.</p> <pre><code>suspend fun main() {\n    val ff4k = ff4k {\n        properties {\n            // String property\n            property(\"api-url\") {\n                value = \"https://api.example.com\"\n            }\n\n            // Integer property with constraints\n            property(\"max-retries\") {\n                value = 3\n                description = \"Max API retries\"\n                fixedValues(1, 3, 5) // Value must be one of these\n                readOnly = true // Prevent runtime modification\n            }\n        }\n    }\n}\n</code></pre>"},{"location":"usage/basics/#checking-features","title":"Checking Features","text":"<p>There are several ways to check the status of a feature.</p>"},{"location":"usage/basics/#boolean-check","title":"Boolean Check","text":"<p>The <code>check</code> method returns a boolean indicating if the feature is enabled.</p> <pre><code>if (ff4k.check(\"dark-mode\")) {\n    enableDarkMode()\n}\n</code></pre>"},{"location":"usage/basics/#functional-style","title":"Functional Style","text":"<p>The <code>ifEnabled</code> and <code>ifEnabledOrElse</code> extension functions provide a more functional approach.</p> <pre><code>// Execute only if enabled\nff4k.ifEnabled(\"dark-mode\") {\n    enableDarkMode()\n}\n\n// Execute one or the other\nff4k.ifEnabledOrElse(\"dark-mode\",\n    enabled = { \n        println(\"Dark mode is ON\") \n    },\n    disabled = { \n        println(\"Dark mode is OFF\") \n    }\n)\n</code></pre>"},{"location":"usage/basics/#managing-groups","title":"Managing Groups","text":"<p>You can perform operations on entire groups of features.</p> <pre><code>// Enable all features in the 'ui-beta' group\nff4k.enableGroup(\"ui-beta\")\n\n// Disable all features in the 'ui-beta' group\nff4k.disableGroup(\"ui-beta\")\n</code></pre>"},{"location":"usage/basics/#runtime-modification","title":"Runtime Modification","text":"<p>If your store supports it (like the default <code>InMemoryFeatureStore</code>), you can modify features at runtime.</p> <pre><code>// Enable a feature\nff4k.enable(\"my-feature\")\n\n// Disable a feature\nff4k.disable(\"my-feature\")\n</code></pre>"},{"location":"usage/basics/#serialization","title":"Serialization","text":"<p>FF4K provides pre-configured serialization support for all built-in types using <code>kotlinx.serialization</code>.</p>"},{"location":"usage/basics/#ff4kjson","title":"FF4kJson","text":"<p><code>FF4kJson</code> is a pre-configured <code>Json</code> instance ready to use for serializing and deserializing FF4K types (features, properties, strategies).</p> <pre><code>// Serialize a feature\nval json = FF4kJson.encodeToString(feature)\n\n// Deserialize a feature\nval feature = FF4kJson.decodeFromString&lt;Feature&gt;(json)\n</code></pre> <p>Configuration: - Includes <code>ff4kSerializersModule</code> for polymorphic serialization of all FF4K types - <code>ignoreUnknownKeys = true</code> for forward compatibility - <code>prettyPrint = true</code> for readable output</p>"},{"location":"usage/basics/#ff4kserializersmodule","title":"ff4kSerializersModule","text":"<p>If you need to customize the <code>Json</code> configuration or combine with your own serializers, use <code>ff4kSerializersModule</code> directly:</p> <pre><code>val customJson = Json {\n    serializersModule = ff4kSerializersModule + myCustomSerializersModule\n    prettyPrint = false // Override defaults as needed\n    encodeDefaults = true\n}\n</code></pre> <p>The module includes serializers for: - All <code>Property</code> subtypes (PropertyInt, PropertyString, PropertyBoolean, etc.) - All <code>FlippingStrategy</code> subtypes (AndStrategy, OrStrategy, PonderationStrategy, etc.)</p> <p>For registering custom types, see Customization.</p>"},{"location":"usage/configuration/","title":"Configuration","text":"<p>FF4K supports loading feature flags and properties from configuration files, making it easy to manage your application's state without changing code.</p>"},{"location":"usage/configuration/#the-configuration-object","title":"The Configuration Object","text":"<p>The <code>FF4kConfiguration</code> class represents the complete state of your FF4K instance, including: - Settings: Global behavior like <code>autoCreate</code>. - Features: Initial feature flags. - Properties: Initial configuration properties.</p>"},{"location":"usage/configuration/#loading-from-json","title":"Loading from JSON","text":"<p>FF4K provides a <code>JsonFF4kConfigurationParser</code> to load configurations from JSON files or resources.</p>"},{"location":"usage/configuration/#example-json","title":"Example JSON","text":"<pre><code>{\n  \"settings\": {\n    \"autoCreate\": true\n  },\n  \"features\": {\n    \"dark-mode\": {\n      \"uid\": \"dark-mode\",\n      \"isEnabled\": true,\n      \"description\": \"Enable dark mode UI\"\n    }\n  },\n  \"properties\": {\n    \"api-timeout\": {\n      \"type\": \"int\",\n      \"name\": \"api-timeout\",\n      \"value\": 5000\n    }\n  }\n}\n</code></pre>"},{"location":"usage/configuration/#loading-resources","title":"Loading Resources","text":"<p>You can load a configuration file bundled with your application using <code>parseConfigurationResource</code>.</p> <pre><code>val parser = JsonFF4kConfigurationParser()\nval config = parser.parseConfigurationResource(\"ff4k_config.json\")\n</code></pre> <p>Platform Specifics: - JVM/Android: Loads from the classpath (e.g., <code>src/main/resources</code>). - Native: Searches in <code>$FF4K_RESOURCES_PATH/&lt;path&gt;</code>, then the current directory, then <code>resources/&lt;path&gt;</code>.</p>"},{"location":"usage/configuration/#loading-files","title":"Loading Files","text":"<p>You can load a configuration file from the filesystem using <code>parseConfigurationFile</code>.</p> <pre><code>val parser = JsonFF4kConfigurationParser()\nval config = parser.parseConfigurationFile(\"/etc/ff4k/config.json\")\n</code></pre> <p>This method supports absolute paths and relative paths (resolved against the current working directory). Tilde (<code>~</code>) expansion is supported for user home directories.</p> <p>Note: - Tilde expansion only supports <code>~</code> and <code>~/...</code> (not <code>~username</code>). - An <code>IllegalArgumentException</code> is thrown if the home directory cannot be determined. - Paths containing directory traversal sequences (<code>..</code>) are rejected and will cause an <code>IllegalArgumentException</code>.</p>"},{"location":"usage/configuration/#initializing-ff4k","title":"Initializing FF4K","text":"<p>Once you have an <code>FF4kConfiguration</code> object, you can use it to initialize your stores.</p> <pre><code>val parser = JsonFF4kConfigurationParser()\nval config = parser.parseConfigurationResource(\"ff4k.json\")\n\nval ff4k = ff4k(\n    featureStore = InMemoryFeatureStore(config),\n    propertyStore = InMemoryPropertyStore(config),\n    autoCreate = config.settings.autoCreate\n)\n</code></pre>"},{"location":"usage/configuration/#auditing-source","title":"Auditing Source","text":"<p>You can optionally specify the <code>source</code> parameter when initializing <code>FF4k</code> to indicate the origin of API calls. This is useful for auditing or logging purposes in your application logic.</p> <pre><code>val ff4k = ff4k(\n    source = FF4k.Source.WebApi // or KotlinApi, EmbeddedServlet, Ssh\n) {\n    // ...\n}\n</code></pre> <p>This value is informational and does not affect feature evaluation logic.</p>"},{"location":"usage/configuration/#exporting-configuration","title":"Exporting Configuration","text":"<p>You can also export the current configuration state to a JSON string, which is useful for debugging or persisting runtime changes.</p> <pre><code>suspend fun main() {\n    val config = FF4kConfiguration(\n        settings = FF4kSettings(autoCreate = ff4k.autoCreate),\n        features = ff4k.features(),\n        properties = ff4k.properties()\n    )\n\n    val json = parser.export(config)\n    File(\"snapshot.json\").writeText(json)\n}\n</code></pre>"},{"location":"usage/monitoring/","title":"Monitoring and Reporting","text":"<p>FF4K provides built-in tools to inspect the state of your feature flags at runtime. This is useful for debugging, logging, and building administrative dashboards.</p>"},{"location":"usage/monitoring/#feature-statistics","title":"Feature Statistics","text":"<p>The <code>stats()</code> method returns a snapshot of your feature store, providing aggregated counts of features by state.</p> <pre><code>val stats = ff4k.stats()\n\nprintln(\"Total features: ${stats.total}\")\nprintln(\"Enabled: ${stats.enabled}\")\nprintln(\"Disabled: ${stats.disabled}\")\nprintln(\"With Permissions: ${stats.withPermissions}\")\nprintln(\"With Strategy: ${stats.withStrategy}\")\nprintln(\"Total Groups: ${stats.groups}\")\n</code></pre>"},{"location":"usage/monitoring/#text-report","title":"Text Report","text":"<p>For a quick human-readable overview, use the <code>report()</code> method. It generates a formatted string listing all features with their status, group, permissions, and strategies.</p> <pre><code>println(ff4k.report())\n</code></pre> <p>Output Example: <pre><code>FF4K Feature Report\n===================\nTotal: 5 | Enabled: 3 | Disabled: 2\nWith Permissions: 2 | With Strategy: 1 | Groups: 2\n\nFeatures:\n  [ON]  dark-mode (group: ui)\n  [OFF] beta-feature (group: ui) [STRATEGY: PercentageStrategy]\n  [ON]  premium (group: billing) [PERMISSIONS: ADMIN, PREMIUM]\n  [OFF] legacy\n  [ON]  new-checkout [STRATEGY: RegionStrategy] [PERMISSIONS: BETA]\n</code></pre></p>"},{"location":"usage/monitoring/#filtering-features","title":"Filtering Features","text":"<p>You can also query specific subsets of features:</p> <ul> <li><code>ff4k.enabledFeatures()</code>: List of all enabled features.</li> <li><code>ff4k.disabledFeatures()</code>: List of all disabled features.</li> <li><code>ff4k.featuresWithPermission(\"ADMIN\")</code>: Features requiring a specific permission.</li> <li><code>ff4k.featuresWithStrategy()</code>: Features that have a flipping strategy attached.</li> </ul>"},{"location":"usage/properties/","title":"Properties","text":"<p>Properties in FF4K allow you to manage configuration values alongside your feature flags. Unlike feature flags which are boolean, properties can hold values of various types.</p>"},{"location":"usage/properties/#built-in-property-types","title":"Built-in Property Types","text":"<p>FF4K comes with support for common data types out of the box:</p>"},{"location":"usage/properties/#primitives","title":"Primitives","text":"<ul> <li><code>PropertyString</code></li> <li><code>PropertyInt</code></li> <li><code>PropertyLong</code></li> <li><code>PropertyDouble</code></li> <li><code>PropertyFloat</code></li> <li><code>PropertyBoolean</code></li> <li><code>PropertyByte</code></li> <li><code>PropertyShort</code></li> </ul>"},{"location":"usage/properties/#big-numbers","title":"Big Numbers","text":"<ul> <li><code>PropertyBigInteger</code></li> <li><code>PropertyBigDecimal</code></li> </ul>"},{"location":"usage/properties/#date-time","title":"Date &amp; Time","text":"<ul> <li><code>PropertyLocalDate</code></li> <li><code>PropertyLocalDateTime</code></li> <li><code>PropertyInstant</code></li> </ul>"},{"location":"usage/properties/#miscellaneous","title":"Miscellaneous","text":"<ul> <li><code>PropertyLogLevel</code></li> </ul>"},{"location":"usage/properties/#usage","title":"Usage","text":""},{"location":"usage/properties/#defining-properties-in-dsl","title":"Defining Properties in DSL","text":"<p>You can define properties within the <code>properties</code> block of the <code>ff4k</code> DSL.</p> <pre><code>suspend fun main() {\n    val ff4k = ff4k {\n        properties {\n            property(\"app-title\") {\n                value = \"My Awesome App\"\n            }\n\n            property(\"max-connections\") {\n                value = 10\n                description = \"Maximum number of concurrent connections\"\n            }\n        }\n    }\n}\n</code></pre>"},{"location":"usage/properties/#retrieving-properties","title":"Retrieving Properties","text":"<p>To retrieve a property, use the <code>property</code> method. You should specify the expected type.</p> <pre><code>// Get the property object\nval titleProp: Property&lt;String&gt;? = ff4k.property&lt;String&gt;(\"app-title\")\nprintln(\"Title: ${titleProp?.value}\")\n\n// Get value directly (safe)\nval maxConns: Int? = ff4k.property&lt;Int&gt;(\"max-connections\")?.value\n</code></pre>"},{"location":"usage/properties/#creating-custom-properties","title":"Creating Custom Properties","text":"<p>If the built-in types don't meet your needs, you can create custom property types by implementing the <code>Property&lt;T&gt;</code> interface or extending <code>AbstractProperty&lt;T&gt;</code>.</p>"},{"location":"usage/properties/#1-implement-the-interface","title":"1. Implement the Interface","text":"<p>You need to implement <code>Property&lt;T&gt;</code>. It's recommended to make your implementation a data class and include serialization support.</p> <pre><code>@Serializable\ndata class PropertyColor(\n    override val name: String,\n    override val value: String, // Storing hex code as string\n    override val description: String? = null,\n    override val fixedValues: Set&lt;String&gt; = emptySet(),\n    override val readOnly: Boolean = false\n) : Property&lt;String&gt;\n</code></pre>"},{"location":"usage/properties/#2-using-custom-properties","title":"2. Using Custom Properties","text":"<p>You can manually add custom properties to the store. Here is a complete example of how to register and use a custom property type:</p> <pre><code>suspend fun main() {\n    // 1. Initialize your property store\n    val myPropertyStore = InMemoryPropertyStore()\n\n    // 2. Initialize FF4K with your store\n    val ff4k = ff4k(propertyStore = myPropertyStore) {\n        // ... other configuration ...\n    }\n\n    // 3. Create your custom property\n    val brandColor = PropertyColor(\n        name = \"brand-color\",\n        value = \"#FF5722\",\n        description = \"Primary brand color\"\n    )\n\n    // 4. Add it to the store manually\n    myPropertyStore += brandColor\n\n    // 5. Retrieve and use it\n    val storedColor = ff4k.property&lt;String&gt;(\"brand-color\")\n    println(\"Brand Color: ${storedColor?.value}\") // Output: #FF5722\n}\n</code></pre>"},{"location":"usage/strategies/","title":"Flipping Strategies","text":"<p>Flipping strategies allow you to control when a feature is enabled based on dynamic conditions, rather than just a simple on/off toggle.</p>"},{"location":"usage/strategies/#overview","title":"Overview","text":"<p>A <code>FlippingStrategy</code> is evaluated each time a feature is checked. This enables use cases like: - Gradual rollouts (percentage-based) - User-specific targeting - A/B testing - Time-based feature availability</p>"},{"location":"usage/strategies/#execution-context","title":"Execution Context","text":"<p>Most strategies require runtime context (e.g., who is the current user?) to make decisions. You can pass this context using <code>FlippingExecutionContext</code>.</p> <p>FF4K provides standard keys in <code>ContextKeys</code> for common parameters such as:</p> <ul> <li><code>ContextKeys.USER_ID</code> (\"userId\"): Unique identifier for the user. Required for user-based strategies.</li> <li><code>ContextKeys.USER_NAME</code> (\"userName\"): User's display name.</li> <li><code>ContextKeys.REGION</code> (\"region\"): Geographic region (e.g., \"EU\", \"US\").</li> <li><code>ContextKeys.ENVIRONMENT</code> (\"environment\"): App environment (e.g., \"prod\", \"staging\").</li> <li><code>ContextKeys.CLIENT_HOSTNAME</code> (\"clientHostname\"): Client hostname making the request.</li> <li><code>ContextKeys.SERVER_HOSTNAME</code> (\"serverHostname\"): Server hostname handling the request.</li> </ul> <pre><code>val context = FlippingExecutionContext(\n    ContextKeys.USER_ID to \"user-123\",\n    ContextKeys.REGION to \"EU\"\n)\n\nff4k.check(\"my-feature\", context)\n</code></pre>"},{"location":"usage/strategies/#constant-strategies","title":"Constant Strategies","text":""},{"location":"usage/strategies/#alwaystrueflippingstrategy","title":"AlwaysTrueFlippingStrategy","text":"<p>Always returns <code>true</code>. Useful as a base case in composite strategies.</p>"},{"location":"usage/strategies/#alwaysfalseflippingstrategy","title":"AlwaysFalseFlippingStrategy","text":"<p>Always returns <code>false</code>. Useful as a base case in composite strategies.</p>"},{"location":"usage/strategies/#percentage-based-strategies","title":"Percentage-Based Strategies","text":""},{"location":"usage/strategies/#ponderationstrategy","title":"PonderationStrategy","text":"<p>Enables a feature based on a random percentage. Each evaluation is independent - the same user may get different results on subsequent calls.</p> <pre><code>feature(\"gradual-rollout\") {\n    ponderationStrategy(0.25) // 25% chance of being enabled\n}\n\n// Or using integer percentage\nfeature(\"gradual-rollout\") {\n    ponderationStrategy(25) // 25% chance\n}\n</code></pre>"},{"location":"usage/strategies/#userponderationstrategy","title":"UserPonderationStrategy","text":"<p>Enables a feature for a consistent percentage of users. The same user always gets the same result (sticky sessions), making it ideal for A/B testing and gradual rollouts.</p> <p>Requires <code>ContextKeys.USER_ID</code> in the execution context.</p> <pre><code>feature(\"beta-feature\") {\n    userPonderationStrategy(0.10) // 10% of users\n}\n\n// Or using integer percentage\nfeature(\"beta-feature\") {\n    userPonderationStrategy(10) // 10% of users\n}\n\n// Checking with user context\nval context = FlippingExecutionContext(ContextKeys.USER_ID to currentUserId)\nff4k.check(\"beta-feature\", context)\n</code></pre>"},{"location":"usage/strategies/#user-list-strategies","title":"User-List Strategies","text":""},{"location":"usage/strategies/#allowliststrategy","title":"AllowListStrategy","text":"<p>Enables a feature only for specific users. All other users will have the feature disabled.</p> <p>Requires <code>ContextKeys.USER_ID</code> in the execution context.</p> <pre><code>feature(\"vip-feature\") {\n    allowListStrategy {\n        +\"user-123\"\n        +\"user-456\"\n        add(\"user-789\")\n    }\n}\n</code></pre>"},{"location":"usage/strategies/#denyliststrategy","title":"DenyListStrategy","text":"<p>Disables a feature for specific users. All other users will have the feature enabled.</p> <p>Requires <code>ContextKeys.USER_ID</code> in the execution context.</p> <pre><code>feature(\"new-ui\") {\n    denyListStrategy {\n        +\"problematic-user-1\"\n        +\"problematic-user-2\"\n    }\n}\n</code></pre>"},{"location":"usage/strategies/#filter-strategies","title":"Filter Strategies","text":""},{"location":"usage/strategies/#clientfilterstrategy","title":"ClientFilterStrategy","text":"<p>Enables a feature only for requests from specific client hostnames.</p> <p>Requires <code>ContextKeys.CLIENT_HOSTNAME</code> in the execution context.</p> <pre><code>feature(\"internal-only\") {\n    clientFilterStrategy {\n        +\"client-a.internal.com\"\n        +\"client-b.internal.com\"\n    }\n}\n\n// Checking with client context\nval context = FlippingExecutionContext(ContextKeys.CLIENT_HOSTNAME to request.hostname)\nff4k.check(\"internal-only\", context)\n</code></pre>"},{"location":"usage/strategies/#serverfilterstrategy","title":"ServerFilterStrategy","text":"<p>Enables a feature only on specific server hostnames. Useful for canary deployments or testing features on specific server instances.</p> <p>Requires <code>ContextKeys.SERVER_HOSTNAME</code> in the execution context.</p> <pre><code>feature(\"canary-feature\") {\n    serverFilterStrategy {\n        +\"server-1.prod.com\"\n        +\"server-2.prod.com\"\n    }\n}\n\n// Checking with server context\nval context = FlippingExecutionContext(ContextKeys.SERVER_HOSTNAME to System.getenv(\"HOSTNAME\"))\nff4k.check(\"canary-feature\", context)\n</code></pre>"},{"location":"usage/strategies/#regionfilterstrategy","title":"RegionFilterStrategy","text":"<p>Enables a feature only for specific geographic regions.</p> <p>Requires <code>ContextKeys.REGION</code> in the execution context.</p> <pre><code>feature(\"eu-only\") {\n    regionStrategy {\n        +\"eu-central-1\"\n        +\"eu-west-1\"\n    }\n}\n\n// Checking with region context\nval context = FlippingExecutionContext(ContextKeys.REGION to currentRegion)\nff4k.check(\"eu-only\", context)\n</code></pre>"},{"location":"usage/strategies/#time-based-strategies","title":"Time-Based Strategies","text":""},{"location":"usage/strategies/#releasedatestrategy","title":"ReleaseDateStrategy","text":"<p>Enables a feature after a specified release date. The feature is disabled before the release date and enabled once the current time reaches or passes it.</p> <p>Useful for scheduling feature launches without requiring a deployment.</p> <pre><code>// Using ISO-8601 string\nfeature(\"new-feature\") {\n    releaseDateStrategy(\"2025-06-01T00:00:00Z\")\n}\n\n// Using Instant\nfeature(\"new-feature\") {\n    releaseDateStrategy(Instant.parse(\"2025-06-01T00:00:00Z\"))\n}\n\n// Using LocalDateTime with timezone\nfeature(\"new-feature\") {\n    releaseDateStrategy(\n        dateTime = LocalDateTime(2025, 6, 1, 9, 0),\n        timezone = TimeZone.of(\"America/New_York\")\n    )\n}\n</code></pre>"},{"location":"usage/strategies/#daterangestrategy","title":"DateRangeStrategy","text":"<p>Enables a feature only within a specified time range. The feature is enabled when the current time is at or after the start date and before the end date (i.e., <code>[startDate, endDate)</code>).</p> <p>Useful for limited-time promotions, scheduled maintenance windows, or time-boxed experiments.</p> <pre><code>// Using ISO-8601 strings\nfeature(\"holiday-sale\") {\n    dateRangeStrategy(\n        startDate = \"2025-12-20T00:00:00Z\",\n        endDate = \"2025-12-26T00:00:00Z\"\n    )\n}\n\n// Using Instant values\nfeature(\"maintenance-mode\") {\n    dateRangeStrategy(\n        startDate = maintenanceStart,\n        endDate = maintenanceEnd\n    )\n}\n\n// Using LocalDateTime with timezone\nfeature(\"summer-promo\") {\n    dateRangeStrategy(\n        startDate = LocalDateTime(2025, 6, 1, 0, 0),\n        endDate = LocalDateTime(2025, 8, 31, 23, 59),\n        timezone = TimeZone.of(\"Europe/London\")\n    )\n}\n</code></pre>"},{"location":"usage/strategies/#dailyhoursstrategy","title":"DailyHoursStrategy","text":"<p>Enables a feature only during specific hours of the day. The feature is enabled when the current hour is within the specified range: <code>[startHour, endHour)</code> (start inclusive, end exclusive).</p> <p>Useful for features that should only be available during business hours, off-peak times, or any recurring daily time window.</p> <pre><code>// Enable feature from 9 AM to 5 PM UTC\nfeature(\"business-hours\") {\n    dailyHoursStrategy(\n        startHour = 9,\n        endHour = 17\n    )\n}\n\n// Enable feature during off-peak hours (10 PM to 00 AM) in a specific timezone\nfeature(\"off-peak-processing\") {\n    dailyHoursStrategy(\n        startHour = 22,\n        endHour = 24,\n        timezone = TimeZone.of(\"America/New_York\")\n    )\n}\n</code></pre>"},{"location":"usage/strategies/#weekdaystrategy","title":"WeekdayStrategy","text":"<p>Enables a feature only on specific days of the week. The feature is enabled when the current day matches one of the allowed days.</p> <p>Useful for features that should only be available on certain days, such as weekday-only features, weekend promotions, or scheduled maintenance windows.</p> <pre><code>// Enable feature on weekdays only\nfeature(\"weekday-feature\") {\n    weekdayStrategy {\n        weekdays() // Monday through Friday\n    }\n}\n\n// Enable feature on weekends only\nfeature(\"weekend-promo\") {\n    weekdayStrategy {\n        weekends() // Saturday and Sunday\n    }\n}\n\n// Enable feature on specific days\nfeature(\"tuesday-thursday\") {\n    weekdayStrategy(TimeZone.of(\"Europe/London\")) {\n        +DayOfWeek.TUESDAY\n        +DayOfWeek.THURSDAY\n    }\n}\n\n// Enable feature every day (useful when combined with other strategies)\nfeature(\"all-week\") {\n    weekdayStrategy {\n        allDays()\n    }\n}\n</code></pre>"},{"location":"usage/strategies/#composite-strategies","title":"Composite Strategies","text":"<p>Combine multiple strategies using logical operators.</p>"},{"location":"usage/strategies/#andstrategy","title":"AndStrategy","text":"<p>All strategies must evaluate to <code>true</code>.</p> <pre><code>val strategy = strategyA and strategyB // Both must be true\n</code></pre>"},{"location":"usage/strategies/#orstrategy","title":"OrStrategy","text":"<p>At least one strategy must evaluate to <code>true</code>.</p> <pre><code>val strategy = strategyA or strategyB // Either can be true\n</code></pre>"},{"location":"usage/strategies/#notstrategy","title":"NotStrategy","text":"<p>Inverts the result of a strategy.</p> <pre><code>val strategy = !strategyA // Inverts the result\n</code></pre>"},{"location":"usage/strategies/#combining-strategies","title":"Combining Strategies","text":"<p>You can chain multiple operators to create complex conditions:</p> <pre><code>feature(\"complex-feature\") {\n    flippingStrategy = (strategyA and strategyB) or !strategyC\n}\n</code></pre> <p>For creating custom strategies, see Customization.</p>"}]}